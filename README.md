x86_64 安装 openwrt教程 及 ech-wk 配置
=======================

以下所有软件包下载地址
x86_64 openwrt.zip <br>
链接: https://pan.baidu.com/s/1Dm2XcaVfS-LPkcfuGRpn9w?pwd=3qq3 提取码: 3qq3

# 刷机安装openwrt
---

## ✅ 一、准备工作

### 1. 确认你的 t430 型号
- **HP t430 Thin Client**（注意不是服务器 T430）
- CPU：通常为 Intel Celeron J4125 / N5105 / N4100 等（x86_64 架构）
- 内存：4GB 或 8GB（足够运行 OpenWrt）
- 存储：32GB/64GB eMMC 或 M.2 SSD
- 网口：1 个（部分型号可加 USB 网卡）

> ✅ 只要是 **x86_64 架构**，就能刷 OpenWrt！

---

### 2. 下载所需文件和工具

| 项目 | 说明 |
|------|------|
| **OpenWrt 镜像** | 官网下载：[https://downloads.openwrt.org](https://downloads.openwrt.org) → `x86/64` → 选择 `generic`<br>推荐文件：<br>`openwrt-x86-64-generic-squashfs-combined.img.gz`（用于 U 盘启动）<br>`openwrt-x86-64-generic-squashfs-rootfs.img.gz`（仅根文件系统） |
| **写盘工具** | [balenaEtcher](https://www.balena.io/etcher/)（图形化，最简单）<br>或 `dd` 命令（Linux/macOS） |
| **U 盘** | ≥4GB，**所有数据会被清空！** |
| **显示器+键盘** | 初次安装需要（后续可通过 SSH 管理） |

---

## 🛠 二、制作 OpenWrt 启动 U 盘

### Windows 用户（用 Etcher）：
1. 解压下载的 `.img.gz` 文件（用 7-Zip 或 WinRAR），得到 `.img` 文件。
2. 打开 **balenaEtcher**。
3. 选择 `.img` 文件 → 选择 U 盘 → 点击 **Flash!**
4. 等待完成，安全弹出 U 盘。

### Linux/macOS 用户（命令行）：
```bash
# 查看 U 盘设备名（如 /dev/sdb）
lsblk

# 写入镜像（替换 sdb 为你的 U 盘）
sudo dd if=openwrt-x86-64-generic-squashfs-combined.img of=/dev/sdb bs=4M status=progress oflag=sync
```

> ⚠️ 务必确认设备名，写错会清空电脑硬盘！

---

## 💻 三、从 U 盘启动 t430 并安装到本地存储

### 1. 设置 BIOS
- 开机按 **F10** 进入 BIOS
- 关闭 **Secure Boot**
- 设置 **U 盘为第一启动项**
- 保存退出

### 2. 启动 OpenWrt 临时系统
- 插入 U 盘，开机
- 系统会自动从 U 盘加载 OpenWrt（约 30 秒）
- 屏幕显示 `OpenWrt` 登录提示符（无图形界面）

### 3. 登录并安装到 eMMC/SSD
默认账号：`root`，密码为空。

```bash
# 查看本地磁盘（通常是 /dev/mmcblk0 或 /dev/sda）
lsblk

# 假设本地存储是 /dev/mmcblk0（eMMC）或 /dev/sda（SSD）
# 将 U 盘中的系统完整写入本地磁盘
dd if=/dev/sdb of=/dev/mmcblk0 bs=4M status=progress
# 或
dd if=/dev/sdb of=/dev/sda bs=4M status=progress
```

> 🔍 如何判断 U 盘和本地盘？
> - U 盘大小 ≈ 你用的 U 盘容量（如 16GB）
> - 本地 eMMC 通常是 32GB/64GB
> - 用 `cat /proc/partitions` 辅助判断

### 4. 等待写入完成（5–10 分钟）
- 不要断电！
- 完成后拔掉 U 盘，重启

---

## ⚙️ 四、首次配置 OpenWrt（基础网络设置）

### 1. 连接网络
- 用网线将 t430 连接到你的路由器 LAN 口
- OpenWrt 默认开启 DHCP 客户端，会自动获取 IP

### 2. 查找 IP 地址
- 登录主路由后台（如 192.168.1.1）
- 查看 DHCP 客户列表，找到名为 `OpenWrt` 的设备，记下 IP（如 `192.168.1.100`）

### 3. 登录 Web 管理界面（LuCI）
- 浏览器访问：`http://192.168.1.100`
- 用户名：`root`，密码：**第一次登录需设置**

### 4. 基础配置建议
| 项目 | 推荐设置 |
|------|--------|
| **LAN 口 IP** | 改为 `192.168.2.1`（避免与主路由冲突，尤其做旁路由时） |
| **关闭 DHCP** | 如果做旁路由，必须关闭！ |
| **启用 SSH** | 系统 → 管理权 → 设置密码并启用 SSH |
| **时区** | 系统 → 系统 → 时区设为 `Asia/Shanghai` |

---

## 🧩 五、扩展功能（解决空间小问题）

t430 通常有 32GB+ eMMC，**完全不用担心空间不足**！  
但为了更高效使用：

### 安装插件（通过 LuCI 或 SSH）：
```bash
opkg update
opkg install luci-app-adguardhome luci-app-passwall2 curl wget
```

> ✅ t430 的 eMMC 空间远大于 AX3600，可随意安装插件！

---

## 🔄 六、恢复原系统（如果需要）
- t430 原系统是 Windows IoT 或 ThinPro
- 想恢复？需 HP 官方镜像 + Rufus 写入 U 盘重装
- OpenWrt **不会破坏原系统分区表**（除非你手动格式化整个盘）

---

## ✅ 总结

| 优势 | 说明 |
|------|------|
| **性能强** | 多核 CPU + 大内存，跑满千兆宽带无压力 |
| **空间大** | 32GB+ eMMC，插件随便装 |
| **功耗低** | 待机 4–6W，适合 24/7 运行 |
| **可做主路由/旁路由** | 单网口建议做旁路由；加 USB 网卡可做主路由 |

---

### 🔗 推荐资源
- OpenWrt 官方 x86 镜像：https://downloads.openwrt.org/releases/23.05.3/targets/x86/64/
- 恩山论坛 t430 专区：搜索 “HP t430 OpenWrt”
- USB 网卡推荐：绿联 CM425（ASIX AX88179 芯片，即插即用）

---

如果你告诉我你的 t430 具体配置（CPU、网口数量、是否有 USB 网卡），我可以提供更定制化的建议！


# 安装ech-wk插件

当然可以！以下是将上述内容整理为 **Microsoft Word 文档（.docx）格式的完整文本结构**，你可以直接复制到 Word 中，或使用 Python 的 `python-docx` 库自动生成文档。文末也提供了一个可直接下载的 `.docx` 文件生成建议。

---

# **luci-app-ech-workers 插件安装与配置指南**

> 项目地址：https://github.com/SunshineList/luci-app-ech-workers  
> 最后更新：2025 年 12 月

---

## 一、安装方法

### 1. 下载插件 IPK 包
- 访问 [Releases 页面](https://github.com/SunshineList/luci-app-ech-workers/releases)
- 下载最新版文件：  
  `luci-app-ech-workers_x.x.x_all.ipk`

> **重要提示**：  
> 安装此 `.ipk` 后，插件会自动检测路由器架构（如 aarch64、x86_64 等），并从 GitHub 自动下载对应架构的 `ech-workers` 二进制文件到 `/usr/bin/ech-workers`，无需手动安装核心程序！

### 2. 安装步骤（通过 SSH）

```bash
# 1. 将 IPK 文件上传到路由器 /tmp 目录（本地执行）
scp luci-app-ech-workers_*.ipk root@192.168.1.1:/tmp/

# 2. SSH 登录路由器
ssh root@192.168.1.1

# 3. 安装插件
opkg install /tmp/luci-app-ech-workers_*.ipk
```

> **注意**：  
> 自动下载 `ech-workers` 二进制文件需要路由器能访问 GitHub。  
> 若下载失败（如网络受限），请手动下载对应架构的二进制文件，并放置到：
> ```
> /usr/bin/ech-workers
> ```
> 然后赋予执行权限：
> ```bash
> chmod +x /usr/bin/ech-workers
> ```

---

## 二、访问 Web 界面

- 打开浏览器，登录 OpenWrt 管理界面（通常为 `http://192.168.1.1`）
- 导航路径：  
  **服务 → Tuple ECH Worker**

---

## 三、配置说明

### 1. 基本设置

| 配置项 | 说明 | 示例值 |
|--------|------|--------|
| 启用 | 开启/关闭服务 | ✅ 勾选 |
| 服务器地址 | Workers 服务端地址（由 Cloudflare 提供） | `your-worker.workers.dev:443` |
| 监听地址 | 本地代理监听端口 | `0.0.0.0:30001` |
| 身份令牌 | 服务端验证密钥（可选） | （留空或填写 Token） |

### 2. 高级设置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 优选 IP/域名 | Cloudflare CDN 优选地址（加速连接） | `cf.090227.xyz` |
| DoH 服务器 | DNS over HTTPS 服务器 | `dns.alidns.com/dns-query` |
| ECH 域名 | 用于获取 ECH 配置（Encrypted Client Hello） | `cloudflare-ech.com` |

### 3. 分流模式（必选其一）

| 模式 | 说明 |
|------|------|
| **全局代理** | 所有流量通过代理 |
| **跳过中国大陆** | 国内 IP 直连，境外走代理（**推荐**） |
| **直连模式** | 所有流量直连，不使用代理（仅用于测试） |

---

## 四、客户端使用方式

服务启动后，在终端设备上配置代理：

| 协议 | 地址 | 端口 |
|------|------|------|
| SOCKS5 | 路由器 LAN IP（如 `192.168.1.1`） | `30001` |
| HTTP/HTTPS | 路由器 LAN IP | `30001` |

### 配置示例：
- **Windows**：设置 → 网络 → 代理 → 手动设置 → 地址填路由器 IP，端口 30001  
- **macOS/iOS**：WiFi 高级 → 代理 → 手动 → HTTP/HTTPS/SOCKS 均设为路由器 IP + 30001  
- **浏览器插件**：SwitchyOmega、FoxyProxy 等可单独配置规则

---

## 五、故障排查

### 查看服务状态：
```bash
/etc/init.d/ech-workers status
```

### 查看运行日志：
```bash
logread -e ech-workers | tail -n 50
```

### 手动测试运行：
```bash
/usr/bin/ech-workers -f your-worker.workers.dev:443 -l 0.0.0.0:30001
```

### 常见问题解决：

| 问题 | 解决方案 |
|------|----------|
| 服务无法启动 | 检查服务器地址是否正确；确认 `/usr/bin/ech-workers` 有执行权限 |
| 无法连接代理 | 检查防火墙是否放行 30001 端口；确认监听地址为 `0.0.0.0` |
| 速度慢 | 尝试更换“优选 IP”或“DoH 服务器” |

---

## 六、总结流程

1. **下载 `.ipk` → 上传 → `opkg install`**  
2. **插件自动下载 `ech-workers` 二进制（或手动补全）**  
3. **Web 界面配置服务器、监听端口、分流模式**  
4. **客户端设置代理指向路由器 IP:30001**  
5. **通过日志和服务状态验证运行**

> 该项目基于 [byJoey/ech-wk](https://github.com/byJoey/ech-wk)，支持 **ECH 加密、SOCKS5/HTTP 代理、智能分流**，适合在 OpenWrt 上实现轻量级代理网关。

---

### 附录：相关链接
- 核心项目：https://github.com/byJoey/ech-wk  
- OpenWrt 官网：https://openwrt.org/  
- LuCI 开发文档：https://openwrt.org/docs/guide-developer/luci

---

## 如何生成 Word 文档？

### 方法一：手动复制
1. 全选上方内容（从“# luci-app-ech-workers...”开始）
2. 打开 Microsoft Word 或 WPS
3. 粘贴 → 选择“保留源格式”或“匹配目标格式”
4. 保存为 `.docx` 文件

### 方法二：使用 Python 自动生成（需安装 `python-docx`）
```python
from docx import Document

doc = Document()
doc.add_heading('luci-app-ech-workers 插件安装与配置指南', 0)
doc.add_paragraph('项目地址：https://github.com/SunshineList/luci-app-ech-workers')
# ...（逐段添加内容）
doc.save('luci-app-ech-workers_安装配置指南.docx')
```

> 如需我为你生成一个可直接下载的 `.docx` 文件，请告知（当前环境暂不支持文件输出，但可提供 Base64 或 Markdown 转换方案）。

--- 

✅ **文档结束**